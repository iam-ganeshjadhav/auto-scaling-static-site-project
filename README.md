# auto-scaling-static-site-project
build an auto-scaling infrastructure for the static website using an Application Load Balancer and Auto Scaling Group. Terraform must create the entire infrastructure, and Jenkins must deliver new updates automatically

pipeline {
    agent any

    environment {
        TF_VERSION = "1.7.0"
        AWS_REGION = "ap-south-1"
    }

    stages {

        /* -----------------------------
           CHECKOUT APPLICATION WEBSITE CODE
        ------------------------------ */
        stage('Checkout SCM') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/iam-ganeshjadhav/auto-scaling-static-site-project.git'
            }
        }

        /* -----------------------------
           CHECKOUT TERRAFORM INFRA CODE
        ------------------------------ */
        stage('Checkout Infra Code') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/iam-ganeshjadhav/auto-scaling-static-site-project.git'
            }
        }

        /* -----------------------------
           TERRAFORM INIT
        ------------------------------ */
        stage('Terraform Init') {
            steps {
                withCredentials([[
                    $class: 'AmazonWebServicesCredentialsBinding',
                    credentialsId: 'aws-creds'
                ]]) {
                    sh '''
                        echo "Running Terraform Init..."
                        export AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID
                        export AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY

                        terraform init
                    '''
                }
            }
        }

        /* -----------------------------
           TERRAFORM PLAN
        ------------------------------ */
        stage('Terraform Plan') {
            steps {
                withCredentials([[
                    $class: 'AmazonWebServicesCredentialsBinding',
                    credentialsId: 'aws-creds'
                ]]) {
                    sh '''
                        echo "Running Terraform Plan..."
                        terraform plan
                    '''
                }
            }
        }

        /* -----------------------------
           TERRAFORM APPLY
        ------------------------------ */
        stage('Terraform Apply') {
            steps {
                withCredentials([[
                    $class: 'AmazonWebServicesCredentialsBinding',
                    credentialsId: 'aws-creds'
                ]]) {
                    sh '''
                        echo "Running Terraform Apply..."
                        terraform apply -auto-approve
                    '''
                }
            }
        }

        /* -----------------------------
           VALIDATE WEBSITE BEHIND ALB
        ------------------------------ */
        stage('Validate Website Behind ALB') {
            steps {
                script {
                    echo "Validation placeholder – You can add ALB DNS test here."
                }
            }
        }

        /* -----------------------------
           LOAD TEST PLACEHOLDER
        ------------------------------ */
        stage('Trigger CPU Load Test Placeholder') {
            steps {
                echo "CPU Load test placeholder"
            }
        }
    }
}
